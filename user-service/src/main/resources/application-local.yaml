spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/marketplace_db
    username: marketplace_user
    password: javak

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest